# Copy this file to .env and add your key there. Never commit .env.
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
PAYMENT_INTENT_SECRET=
# Visa sandbox: either enrol a project by hand (see docs/demo-runbook.md) or
# restore a previous enrolment with `source scripts/restore_visa_creds.sh`.
PUDDLE_VISA_BUNDLE=
# Optional: a small LLM phrases the duck's verdict from the math's reasons.
# Leave both empty and the deterministic headline is used.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
